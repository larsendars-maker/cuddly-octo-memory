# Orbit AI

OrbitDesk includes a free built-in helper at `/api/ai/help`. It requires no external API key and gives deterministic help for spreadsheet formulas, table structure and safe code/API examples.

It is intentionally not presented as a full cloud LLM. A real generative model can be connected later through a server-side provider without exposing its key to the browser.

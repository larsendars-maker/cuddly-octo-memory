# MOBA Arena Online — Render Web Service

Files are intentionally in the repository root.

Render settings:
- Runtime: Node
- Build Command: npm install
- Start Command: npm start
- Health Check Path: /health
- Root Directory: leave empty

Do NOT use `npm install` as the Start Command. It only installs packages and then exits.

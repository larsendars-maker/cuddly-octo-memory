import React,{useEffect,useMemo,useRef,useState} from 'react';
import {createRoot} from 'react-dom/client';
import './styles.css';

type User={id:number;username:string;email:string;xp:number;role:string;rank:{name:string;xp:number};email_verified?:boolean};
type Site={id:string;title:string;url:string;icon:string;category:string};
type Tab={id:string;kind:'site'|'table'|'profile'|'settings'|'chat'|'admin'|'integrations'|'history';title:string;url?:string;site?:Site;tableId?:number|string};
type TableDoc={id:number;name:string;payload:{columns:string[];rows:string[][]};updated_at:string};
type Settings={theme:string;accent:string;compact:boolean;blur:number;sidebar:number;roundness:number;wallpaper:string;showClock:boolean;displayName:string;bio:string;avatarUrl:string};

const roleLabel=(r:string)=>r==='admin'?'ADMIN':r==='assistant'?'HELPER':'USER';
function csrf(){return document.cookie.split('; ').find(x=>x.startsWith('od_csrf='))?.split('=')[1]||'';}
async function api(path:string,opts:RequestInit={}){const h=new Headers(opts.headers||{});if(opts.body&&!(opts.body instanceof FormData)&&!h.has('content-type'))h.set('content-type','application/json');if(!['GET','HEAD','OPTIONS'].includes((opts.method||'GET').toUpperCase())&&!path.startsWith('/api/auth/'))h.set('X-CSRF-Token',decodeURIComponent(csrf()));const r=await fetch(path,{credentials:'include',...opts,headers:h});if(!r.ok)throw new Error((await r.json().catch(()=>({error:r.statusText}))).error||'REQUEST_FAILED');return r.json();}

function Auth({onLogin}:{onLogin:(u:User)=>void}){const[mode,setMode]=useState<'login'|'register'>('login'),[login,setLogin]=useState(''),[email,setEmail]=useState(''),[pass,setPass]=useState(''),[err,setErr]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setErr('');try{const d=await api('/api/auth/'+mode,{method:'POST',body:JSON.stringify(mode==='login'?{login,password:pass}:{username:login,email,password:pass})});onLogin(d.user);}catch(e:any){setErr(e.message==='EMAIL_NOT_VERIFIED'?'Почта не подтверждена. Подтверди email и войди снова.':e.message);}}return <div className="auth-shell"><div className="auth-card"><div className="brand">◈ OrbitDesk</div><p className="muted">Workspace, сайты, таблицы и друзья.</p><div className="seg"><button className={mode==='login'?'active':''} onClick={()=>setMode('login')}>Вход</button><button className={mode==='register'?'active':''} onClick={()=>setMode('register')}>Регистрация</button></div><form onSubmit={submit}><input value={login} onChange={e=>setLogin(e.target.value)} placeholder={mode==='login'?'Логин или email':'Логин'} required/>{mode==='register'&&<input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" type="email" required/>}<input value={pass} onChange={e=>setPass(e.target.value)} placeholder="Пароль" type="password" minLength={10} required/><button className="primary wide">{mode==='login'?'Войти':'Создать аккаунт'}</button>{err&&<div className="error">{err}</div>}</form><small className="muted">Пароль: 10+ символов, A-Z, a-z, цифра.</small></div></div>}

function App(){
 const [user,setUser]=useState<User|null>(null),[sites,setSites]=useState<Site[]>([]),[tabs,setTabs]=useState<Tab[]>([]),[active,setActive]=useState('home'),[query,setQuery]=useState('');
 const [settings,setSettings]=useState<Settings>({theme:'midnight',accent:'#68a1ff',compact:false,blur:16,sidebar:270,roundness:14,wallpaper:'',showClock:true,displayName:'',bio:'',avatarUrl:''});
 const [tables,setTables]=useState<TableDoc[]>([]),[friends,setFriends]=useState<any[]>([]),[notice,setNotice]=useState(''),ws=useRef<WebSocket|null>(null);
 useEffect(()=>{api('/api/me').then(setUser).catch(()=>setUser(null))},[]);
 useEffect(()=>{if(!user)return;(async()=>{try{const[cfg,s,tb,fs]=await Promise.all([api('/api/config'),api('/api/settings'),api('/api/tables'),api('/api/friends')]);setSites(cfg.sites||[]);setSettings(x=>({...x,...s}));setTables(tb||[]);setFriends(fs||[]);const stored=await api('/api/tabs').catch(()=>[]);if(Array.isArray(stored))setTabs(stored.map((t:any)=>({id:String(t.id),kind:'site',title:t.title,url:t.url})));}catch{} })();connect();},[user]);
 useEffect(()=>{document.documentElement.dataset.theme=settings.theme;document.documentElement.style.setProperty('--accent',settings.accent);document.documentElement.style.setProperty('--radius',settings.roundness+'px');document.documentElement.style.setProperty('--sidebar',settings.sidebar+'px');document.documentElement.style.setProperty('--blur',settings.blur+'px');if(settings.wallpaper)document.documentElement.style.setProperty('--wallpaper',`url(${settings.wallpaper})`);else document.documentElement.style.removeProperty('--wallpaper');},[settings]);
 function connect(){try{ws.current=new WebSocket(`${location.protocol==='https:'?'wss':'ws'}://${location.host}/ws`);ws.current.onclose=()=>setTimeout(connect,2500);}catch{}}
 if(!user)return <Auth onLogin={setUser}/>;
 async function saveSettings(next:Partial<Settings>){const merged={...settings,...next};setSettings(merged);await api('/api/settings',{method:'PUT',body:JSON.stringify(merged)});toast('Сохранено');}
 function toast(x:string){setNotice(x);setTimeout(()=>setNotice(''),1400)}
 async function persist(next:Tab[]){setTabs(next);await api('/api/tabs',{method:'PUT',body:JSON.stringify({tabs:next.filter(t=>t.kind==='site'&&t.url).map((t,i)=>({title:t.title,url:t.url,position:i}))})}).catch(()=>{});}
 function openSite(site:Site,url=site.url){const id=crypto.randomUUID();const nt=[...tabs,{id,kind:'site' as const,title:site.title,url,site}];persist(nt);setActive(id);api('/api/history',{method:'POST',body:JSON.stringify({url,title:site.title})}).catch(()=>{});}
 function openUrl(){let u=query.trim();if(!u)return;if(!/^https?:\/\//i.test(u))u='https://'+u;openSite({id:'custom',title:u.replace(/^https?:\/\//,'').replace(/\/$/,'').slice(0,32)||'Сайт',url:u,icon:'🌐',category:'custom'},u);}
 function internal(kind:Tab['kind'],title:string,extra:Partial<Tab>={}){const id=`${kind}-${Date.now()}`;setTabs(x=>[...x,{id,kind,title,...extra}]);setActive(id);}
 function closeTab(id:string){const nt=tabs.filter(t=>t.id!==id);if(active===id)setActive('home');persist(nt)}
 async function logout(){await api('/api/auth/logout',{method:'POST'});location.reload()}
 const activeTab=tabs.find(t=>t.id===active);
 return <div className="app"><aside className="sidebar"><div className="brand small">◈ OrbitDesk</div><button className={active==='home'?'nav active':'nav'} onClick={()=>setActive('home')}>⌂ Главная</button><button className="nav" onClick={()=>internal('table','Таблицы')}>▦ Таблицы</button><button className="nav" onClick={()=>internal('integrations','Подключения')}>🔗 Подключения</button><button className="nav" onClick={()=>internal('profile','Профиль')}>◉ Профиль</button><button className="nav" onClick={()=>internal('chat','Чат')}>☏ Чат</button><button className="nav" onClick={()=>internal('history','История')}>🕘 История</button><button className="nav" onClick={()=>internal('settings','Настройки')}>⚙ Настройки</button>{user.role==='admin'&&<button className="nav admin" onClick={()=>internal('admin','Админ')}>🛡 Админ</button>}<div className="sidebar-bottom"><div className="profile"><div className="avatar" style={{backgroundImage:settings.avatarUrl?`url(${settings.avatarUrl})`:undefined}}>{!settings.avatarUrl&&(settings.displayName||user.username)[0]}</div><div><b>{settings.displayName||user.username}</b><span>{roleLabel(user.role)}</span></div></div><button className="ghost" onClick={logout}>Выйти</button></div></aside>
 <main className="main"><div className="topbar"><div className="tabs"><button className={active==='home'?'tab active':'tab'} onClick={()=>setActive('home')}>Домой</button>{tabs.map(t=><button key={t.id} className={active===t.id?'tab active':'tab'} onClick={()=>setActive(t.id)}>{t.title}<span onClick={e=>{e.stopPropagation();closeTab(t.id)}}>×</span></button>)}</div><div className="top-actions"><span className="rank">{user.rank.name}</span><span className="xp">{user.xp} XP</span></div></div>
 <div className="omnibar"><button className="ghost" onClick={()=>window.history.back()}>←</button><button className="ghost" onClick={()=>window.history.forward()}>→</button><input value={activeTab?.kind==='site'?(activeTab.url||''):active==='home'?query:''} onChange={e=>activeTab?.kind==='site'?setTabs(ts=>ts.map(t=>t.id===activeTab.id?{...t,url:e.target.value}:t)):active==='home'&&setQuery(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&active==='home')openUrl()}} placeholder="Введите сайт или URL…"/><button className="primary" onClick={active==='home'?openUrl:()=>setActive(active)}>{activeTab?.kind==='site'?'Обновить':'Открыть'}</button></div>
 {!user.email_verified&&<div className="email-banner">Почта не подтверждена. Проверь письмо или <button onClick={()=>api('/api/email/resend',{method:'POST'}).then(()=>toast('Письмо отправлено')).catch((e:any)=>toast(e.message))}>отправь снова</button>.</div>}
 <section className="content">{active==='home'?<Home user={user} settings={settings} sites={sites} open={openSite} table={()=>internal('table','Таблицы')}/>:activeTab?.kind==='site'?<SiteView tab={activeTab} onUrl={u=>{setTabs(x=>x.map(t=>t.id===activeTab.id?{...t,url:u}:t));api('/api/history',{method:'POST',body:JSON.stringify({url:u,title:activeTab.title})}).catch(()=>{})}}/>:activeTab?.kind==='table'?<TablesView tables={tables} setTables={setTables}/>:activeTab?.kind==='profile'?<ProfileView user={user} settings={settings} save={saveSettings}/>:activeTab?.kind==='settings'?<SettingsView settings={settings} save={saveSettings}/>:activeTab?.kind==='chat'?<ChatView friends={friends} ws={ws}/>:activeTab?.kind==='integrations'?<IntegrationsView onOpenSite={openSite}/>:activeTab?.kind==='history'?<HistoryView/>:activeTab?.kind==='admin'?<AdminView/>:<div className="empty">Вкладка не найдена.</div>}</section>{notice&&<div className="toast">{notice}</div>}</main></div>}

function Home({user,settings,sites,open,table}:{user:User;settings:Settings;sites:Site[];open:(s:Site)=>void;table:()=>void}){return <div className="home"><div className="hero"><div><div className="eyebrow">PERSONAL WORKSPACE</div><h1>Привет, {settings.displayName||user.username}.</h1><p>{settings.bio||'Сайты, таблицы, Google Sheets, история и друзья — всё внутри OrbitDesk.'}</p></div><div className="hero-card"><b>{user.rank.name}</b><span>{user.xp} XP</span></div></div><div className="quick"><div className="section-head"><h2>Быстрые сайты</h2><span className="muted">Только доступные твоей роли</span></div><div className="site-grid">{sites.map(s=><button key={s.id} className="site-card" onClick={()=>open(s)}><span className="icon">{s.icon}</span><div><b>{s.title}</b><small>{s.url}</small></div></button>)}</div></div><div className="info-grid"><button className="info-card action-card" onClick={table}><b>Таблицы</b><p>Свои документы + подключаемые Google Sheets.</p></button><div className="info-card"><b>История</b><p>Каждый открытый сайт сохраняется для твоего аккаунта.</p></div><div className="info-card"><b>Админ</b><p>{user.role==='admin'?'Полный доступ к пользователям, истории и журналу действий.':'Доступ только согласно роли.'}</p></div></div></div>}

function SiteView({tab,onUrl}:{tab:Tab;onUrl:(u:string)=>void}){const[loading,setLoading]=useState(true);useEffect(()=>setLoading(true),[tab.url]);return <div className="site-workspace"><div className="site-toolbar"><div><b>{tab.title}</b><span className="muted"> {loading?'загрузка…':'готово'}</span></div><div className="site-tools"><button className="ghost" onClick={()=>setLoading(true)}>↻</button><button className="ghost" onClick={()=>window.open(tab.url,'_blank','noopener,noreferrer')}>↗ Отдельно</button></div></div><div className="site-layout"><div className="site-frame-wrap"><iframe className="webview" src={tab.url||'about:blank'} title={tab.title} onLoad={()=>setLoading(false)} referrerPolicy="strict-origin-when-cross-origin"/></div><aside className="site-panel"><b>Отслеживание</b><p className="muted smalltxt">Посещение сохраняется в истории OrbitDesk. Сторонний сайт нельзя менять из iframe из-за browser same-origin policy.</p><button className="ghost" onClick={()=>navigator.clipboard?.writeText(tab.url||'')}>Копировать URL</button></aside></div></div>}

function TablesView({tables,setTables}:{tables:TableDoc[];setTables:React.Dispatch<React.SetStateAction<TableDoc[]>>}){const[selected,setSelected]=useState<number|string|null>(tables[0]?.id??null),[saving,setSaving]=useState('');const t=tables.find(x=>String(x.id)===String(selected));useEffect(()=>{if(selected==null&&tables[0])setSelected(tables[0].id)},[tables,selected]);async function create(){const x=await api('/api/tables',{method:'POST',body:JSON.stringify({name:'Новая таблица',payload:{columns:['A','B','C'],rows:[['','',''],['','',''],['','','']]}})});setTables(a=>[x,...a]);setSelected(x.id);}function update(mut:(x:TableDoc)=>void){if(!t)return;setTables(a=>a.map(x=>String(x.id)===String(t.id)?(()=>{const c={...x,payload:{columns:[...x.payload.columns],rows:x.payload.rows.map(r=>[...r])}};mut(c);return c})():x));setSaving('Изменено…');}useEffect(()=>{if(!t||saving!=='Изменено…')return;const id=setTimeout(async()=>{const fresh=tables.find(x=>String(x.id)===String(t.id));if(!fresh)return;const s=await api('/api/tables/'+fresh.id,{method:'PUT',body:JSON.stringify({name:fresh.name,payload:fresh.payload})});setTables(a=>a.map(x=>String(x.id)===String(fresh.id)?s:x));setSaving('Сохранено');setTimeout(()=>setSaving(''),1000)},700);return()=>clearTimeout(id)},[tables,saving,t]);return <div className="table-workspace"><div className="table-sidebar"><div className="section-head"><b>Таблицы</b><button className="primary mini" onClick={create}>＋</button></div>{tables.map(x=><button key={x.id} className={String(x.id)===String(selected)?'table-item active':'table-item'} onClick={()=>setSelected(x.id)}>{x.name}<span>{new Date(x.updated_at).toLocaleTimeString()}</span></button>)}</div><div className="table-editor">{t?<><div className="table-head"><input value={t.name} onChange={e=>update(x=>x.name=e.target.value)}/><span className="muted">{saving||`Обновлено ${new Date(t.updated_at).toLocaleString()}`}</span></div><table><thead><tr>{t.payload.columns.map((c,i)=><th key={i}><input value={c} onChange={e=>update(x=>x.payload.columns[i]=e.target.value)}/></th>)}</tr></thead><tbody>{t.payload.rows.map((r,ri)=><tr key={ri}>{r.map((c,ci)=><td key={ci}><input value={c} onChange={e=>update(x=>x.payload.rows[ri][ci]=e.target.value)}/></td>)}</tr>)}</tbody></table></>:<div className="empty big">Создай таблицу кнопкой ＋</div>}</div></div>}

function ProfileView({user,settings,save}:{user:User;settings:Settings;save:(x:Partial<Settings>)=>Promise<void>}){const[l,setL]=useState(settings);useEffect(()=>setL(settings),[settings]);return <div className="settings-page"><div className="profile-card"><div className="big-avatar" style={{backgroundImage:l.avatarUrl?`url(${l.avatarUrl})`:undefined}}>{!l.avatarUrl&&(l.displayName||user.username)[0]}</div><h1>{l.displayName||user.username}</h1><span className="rank">{roleLabel(user.role)} · {user.rank.name}</span></div><div className="settings-grid"><label>Отображаемое имя<input value={l.displayName} onChange={e=>setL({...l,displayName:e.target.value.slice(0,40)})}/></label><label>Аватар URL<input value={l.avatarUrl} onChange={e=>setL({...l,avatarUrl:e.target.value.slice(0,500)})}/></label><label>Описание<textarea value={l.bio} onChange={e=>setL({...l,bio:e.target.value.slice(0,240)})}/></label><button className="primary" onClick={()=>save(l)}>Сохранить профиль</button></div></div>}
function SettingsView({settings,save}:{settings:Settings;save:(x:Partial<Settings>)=>Promise<void>}){const[s,setS]=useState(settings);return <div className="settings-page"><h1>Настройки</h1><div className="settings-grid"><label>Тема<select value={s.theme} onChange={e=>setS({...s,theme:e.target.value})}><option value="midnight">Midnight</option><option value="graphite">Graphite</option><option value="light">Light</option><option value="amoled">AMOLED</option></select></label><label>Акцент<input type="color" value={s.accent} onChange={e=>setS({...s,accent:e.target.value})}/></label><label>Обои URL<input value={s.wallpaper} onChange={e=>setS({...s,wallpaper:e.target.value})}/></label><label>Sidebar<input type="range" min="220" max="360" value={s.sidebar} onChange={e=>setS({...s,sidebar:Number(e.target.value)})}/></label><label>Blur<input type="range" min="0" max="24" value={s.blur} onChange={e=>setS({...s,blur:Number(e.target.value)})}/></label><button className="primary" onClick={()=>save(s)}>Сохранить</button></div></div>}

function ChatView({friends,ws}:{friends:any[];ws:React.MutableRefObject<WebSocket|null>}){const[f,setF]=useState<any>(friends[0]||null),[m,setM]=useState<any[]>([]),[text,setText]=useState('');useEffect(()=>{if(f)api('/api/messages/'+f.user_id).then(setM).catch(()=>{})},[f]);function send(){if(!f||!text.trim()||!ws.current)return;ws.current.send(JSON.stringify({type:'chat',to:f.user_id,body:text.trim()}));setText('')}return <div className="chat-page"><aside className="chat-list">{friends.map(x=><button className={f?.user_id===x.user_id?'active':''} key={x.user_id} onClick={()=>setF(x)}>{x.username}<small>{x.rank.name}</small></button>)}</aside><section className="chat-main">{f?<><div className="chat-header">{f.username}</div><div className="messages">{m.map((x:any)=><div className="message" key={x.id}><b>{x.sender_name}</b><span>{x.body}</span></div>)}</div><form className="chat-input" onSubmit={e=>{e.preventDefault();send()}}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Сообщение…"/><button className="primary">Отправить</button></form></>:<div className="empty big">Добавь друга</div>}</section></div>}

function IntegrationsView(){const[connected,setConnected]=useState<any[]>([]),[sheets,setSheets]=useState<any[]>([]),[sel,setSel]=useState<any|null>(null),[data,setData]=useState<any[][]>([]),[range,setRange]=useState(''),[error,setError]=useState('');useEffect(()=>{api('/api/integrations').then(setConnected).catch(()=>{})},[]);async function connectGoogle(){window.location.href='/api/integrations/google/start'}async function list(){setError('');try{setSheets(await api('/api/google/sheets'))}catch(e:any){setError(e.message)}}async function openSheet(x:any){const d=await api('/api/google/sheets/'+x.id);setSel(x);setRange(d.range);setData(d.values)}async function save(){if(!sel)return;await api('/api/google/sheets/'+sel.id,{method:'PUT',body:JSON.stringify({range,values:data})})}function cell(r:number,c:number,v:string){setData(a=>{const b=a.map(x=>[...x]);while(!b[r])b.push([]);b[r][c]=v;return b})}return <div className="settings-page"><h1>Подключения</h1><div className="info-card"><b>Google Sheets</b><p className="muted">Подключение через OAuth. Токены хранятся на сервере в зашифрованном виде.</p><button className="primary" onClick={connectGoogle}>Подключить Google</button> <button className="ghost" onClick={list}>Обновить таблицы</button></div>{error&&<div className="error">{error}</div>}<div className="site-grid" style={{marginTop:16}}>{sheets.map(x=><button className="site-card" key={x.id} onClick={()=>openSheet(x)}><span className="icon">📊</span><div><b>{x.name}</b><small>{x.modifiedTime||''}</small></div></button>)}</div>{sel&&<div className="info-card" style={{marginTop:18}}><div className="section-head"><b>{sel.name}</b><button className="primary" onClick={save}>Сохранить в Google</button></div><input className="omnibar" value={range} onChange={e=>setRange(e.target.value)}/><div style={{overflow:'auto'}}><table><tbody>{data.map((r,ri)=><tr key={ri}>{Array.from({length:Math.max(r.length,8)}).map((_,ci)=><td key={ci} style={{border:'1px solid var(--border)'}}><input value={r[ci]||''} onChange={e=>cell(ri,ci,e.target.value)} /></td>)}</tr>)}</tbody></table></div></div>}</div>}
function HistoryView(){const[h,setH]=useState<any[]>([]);useEffect(()=>{api('/api/history').then(setH).catch(()=>{})},[]);return <div className="settings-page"><h1>История посещений</h1>{h.map(x=><div className="info-card" key={x.id} style={{marginBottom:8}}><b>{x.title||x.url}</b><div className="muted">{new Date(x.visited_at).toLocaleString()}</div><small>{x.url}</small></div>)}{!h.length&&<div className="empty">История пуста.</div>}</div>}
function AdminView(){const[tab,setTab]=useState<'users'|'history'|'audit'>('users'),[users,setUsers]=useState<any[]>([]),[history,setHistory]=useState<any[]>([]),[audit,setAudit]=useState<any[]>([]);useEffect(()=>{api('/api/admin/users').then(setUsers).catch(()=>{})},[]);useEffect(()=>{if(tab==='history')api('/api/admin/history').then(setHistory).catch(()=>{});if(tab==='audit')api('/api/admin/audit').then(setAudit).catch(()=>{})},[tab]);async function role(id:number,r:string){const u=await api('/api/admin/users/'+id+'/role',{method:'PUT',body:JSON.stringify({role:r})});setUsers(x=>x.map(a=>a.id===id?u:a))}return <div className="settings-page"><h1>Админ-панель</h1><div className="seg"><button className={tab==='users'?'active':''} onClick={()=>setTab('users')}>Пользователи</button><button className={tab==='history'?'active':''} onClick={()=>setTab('history')}>История</button><button className={tab==='audit'?'active':''} onClick={()=>setTab('audit')}>Журнал</button></div>{tab==='users'&&<div className="admin-table">{users.map(u=><div className="admin-row" key={u.id}><b>{u.username}</b><span>{u.email}</span><select value={u.role} onChange={e=>role(u.id,e.target.value)}><option value="user">USER</option><option value="assistant">HELPER</option><option value="admin">ADMIN</option></select><span>{u.rank.name}</span></div>)}</div>}{tab==='history'&&history.map(x=><div className="info-card" key={x.id} style={{marginBottom:8}}><b>{x.username} — {x.title||'Сайт'}</b><div>{x.url}</div><small>{new Date(x.visited_at).toLocaleString()}</small></div>)}{tab==='audit'&&audit.map(x=><div className="info-card" key={x.id} style={{marginBottom:8}}><b>{x.username||'system'} — {x.action}</b><div className="muted">{JSON.stringify(x.metadata)}</div><small>{new Date(x.created_at).toLocaleString()}</small></div>)}</div>}

createRoot(document.getElementById('root')!).render(<App/>);
